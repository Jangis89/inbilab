# V31 단계 3~4 성능 보고 (2026-08-17)

기준 작업: 감사 기준 3분 영상(1080×1920, 5,320프레임, auto/fast, 자막 영역 4개).
v29 운영 기준 실측: **1,175s**. 단계 3 게이트 목표: P50 ≤ 480s.
데이터: BENCHMARK_V31_all.json (14 runs, 전건 성공 — 실패 0).

## 결과 요약 (전건 성공, total_s)

| 구성 | 런 | min | P50 | max |
|---|---|---|---|---|
| v29 기준 (운영) | 1 | — | 1,175 | — |
| V31 첫판 K=5 (스레드캡 실수) | 1 | — | 1,249.5 | — |
| V31 K=5 warm (run7) | 3 | 1,078 | 1,116.8 | 1,124 |
| stage4a K=5 (NPROC30) | 3 | 1,160 | 1,388 | 1,676 |
| K=10 warm=0 | 2 | 1,115 | — | 1,382 |
| **shm K=10 (최고 기록)** | 3 | **987** | 1,242.7 | 1,297 |
| GPU-plan K=10 | 3 | 1,209 | 1,948 | 2,479 |

## 확정된 사실 (전부 실측)

1. **세그먼트 재설계는 목표 달성**: 일꾼별 전체 재해독 291~320s → 정확 구간 해독 21~61s
   (**6배**), 중간 AI mp4 0개, mergeseg 저장소 왕복(62~98s/seg) 소멸. K=10에서 세그 exec
   136~287s, 세그 구간 순수 실행 ~289s.
2. **품질 게이트 통과**: 5,320프레임 정확 일치, 오디오/해상도 동일, v29 대비 PSNR 38.4dB /
   SSIM 0.986. 해독·crop byte 동일성 테스트 통과. 공유메모리 스윕도 byte 동일(로컬 검증).
3. **plan(감지·마스크)이 최종 병목**: masks 301~680s. 원인 규명 과정:
   - 프로세스 12→30 확대 → 효과 없음 (병목은 프로세스 수 아님)
   - 공유메모리로 pickle 제거 → 소폭 개선(최고 987s 달성)이나 결정타 아님
   - **지배 변수 = 컨테이너 하드웨어 편차**: 같은 코드가 CPU 상자에서 masks 371~680s,
     GPU 상자에서 301~514s. 실행마다 1.8배 차이.
4. **Modal L40S 확보 대기(혼잡)**: 시간대에 따라 GPU 컨테이너 배정에 1~23분 대기 발생
   (GPU-plan 실험에서 실측). Team 플랜(동시 50)도 물리 재고 부족은 못 막음.
   → 운영 안전망(RunPod 예비)이 여전히 중요하다는 방증.

## 게이트 판정과 이유

- **P50 480s: 미달.** byte 동일성(감지 결과 = v29) 제약 아래 관측된 구성 최적 조합의
  이론 하한 ≈ dl 30 + scan 42 + mask_dec 27 + masks 301 + 세그 250(K=12) + finish 100
  ≈ **750s** — 480s는 감지 알고리즘 산출을 바꾸지 않는 한 도달 불가로 판단(측정 근거 상기).
- 즉 480s 도달에는 다음 중 하나가 필요: ① 감지 결과 변경을 허용하는 재설계(저해상도 스캔,
  세그먼트별 마스크 분산 등 — 품질 게이트를 byte 동일 → VMAF/골든영상 방식으로 교체),
  ② 더 빠른 단일 상자(고성능 CPU 전용 인스턴스), ③ 목표 조정(예: P50 900s = v29 대비 -25%).
- 이 선택은 품질 기준 자체를 바꾸는 결정이므로 단독 진행하지 않고 보고한다
  (명세의 '측정으로 결정' 범위를 넘는 게이트 정의 변경).

## best-k12 (plan=CPU상자, K=12, warm=5, runs=3) 결과
- 1,389.2 / 1,493.6 / 1,255.9 → P50 1,389s. masks 593~686s(저녁 시간대 나쁜 상자 연속),
  세그 exec 139~480s(1건 480s = 콜드/혼잡 대기 포함). K=12 확대 이득은 상자 편차에 묻힘.
- **결론 확정**: 설정 조합 튜닝은 수확 체감 한계 도달. 오늘 총 14 run 전건 성공,
  최고 987s(-16%), 통상 1,100~1,400s. 변동의 지배 요인은 코드가 아니라
  Modal 컨테이너 하드웨어 편차와 L40S 혼잡.

## K=12 결과물 품질 재검증
- run #16 (action=verify): 프레임 5,320 정확·오디오·해상도 게이트 — 결과는 Actions 로그 참조.
