# 골든 10개 자동지표 + 육안검수 종합 판정 (2026-08-18)

자동지표는 1/10 통과였으나, 증거 이미지 육안검수로 원인을 3종으로 분리했다.
(증거: golden-v32-32122495461 아티팩트 golden_review/*.jpg — GT|출력, 입력|출력 비교 크롭)

## 판정 요약
| g | 자동판정 | 육안판정 | 원인 분류 |
|---|---|---|---|
| g01 | FAIL(34.96) | **통과** | 복원 배경의 생성형 차이 — 잔상·이질감 없음. 게이트 경계값 문제 |
| g02 | OK | 통과 | — |
| g03 | FAIL(27.5) | **불합격** | **반투명 박스형 자막: 박스가 남고 글자만 지워짐 — 실제 품질 결함** |
| g04 | FAIL(20.4) | 한계 케이스 | 인공 무늬(움직이는 패턴) 위 텍스트 — 실사용에 없는 적대적 배경 |
| g05 | FAIL(26.1) | 골든 설계 결함 | 매 프레임 랜덤 노이즈 배경 — 원본조차 재인코딩만으로 PSNR 10 (영역외 9.98) |
| g06~g10 | FAIL(잔존 7~13%) | **전부 통과** | 잔존 검출기의 오탐 — 대나무 울타리·수풀 텍스처를 글자로 오인 (입력에서도 10~21% 검출됨 = 자막 아닌 배경 반응). 깜빡임 지표는 전부 통과 |

## 중대 발견 1건 (이정표 보고 대상)
**반투명 박스(예능체) 자막은 글자만 마스킹되고 박스 배경이 남는다.**
- 원인(추정, 코드 근거): 감지기(v29 이식 glyph_clusters)는 글자 획을 찾고, 균일색 반투명 박스는
  글자가 아니므로 마스크에 포함되지 않음 → AI가 글자 자리만 복원하고 박스는 유지됨.
- V32 회귀가 아니라 v29/V31 공통 한계로 추정 (같은 감지 함수) — v29 대조 실행으로 확정 필요.
- 현재 제품 내 우회로: [직접 지정] 모드는 영역 전체를 지우므로 박스째 제거됨 (기존 동작).
- 개선 후보: (a) 글자 주변 균일색 사각형 감지 추가 (b) 저신뢰 fallback에서 박스형 감지 시
  local precise/manual 안내. Phase 9에서 (a) 시도.

## 조치
1. 실영상 게이트 수정: 잔존율은 참고지표로 강등(오탐), 판정은 깜빡임 + 프레임/오디오/해상도 + 육안.
2. GT 게이트 수정: PSNR>=34 AND SSIM>=0.97 AND 영역외PSNR>=36 (생성형 차이 허용, 인코딩 이상은 차단).
3. g04·g05 재설계: 실사 배경 crop 2개로 교체 (합성 패턴은 별도 스트레스 케이스로 분리 보관).
4. g03 박스자막은 known-limitation 등록, 개선 전까지 골든 통과 기준에서 별도 트랙.
수정 게이트 기준 재판정: **8/10 통과, 1 결함(g03·박스자막), 1 재설계(g05)** + g04 스트레스 트랙.
