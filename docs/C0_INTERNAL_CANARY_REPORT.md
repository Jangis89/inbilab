# C0 내부 canary 보고 (Phase J — 2026-08-19)

C0 정의(명세): 운영 고객 트래픽 0%, 관리자 계정 작업만 V32로 처리, deep audit 비율 높게.

## 수행된 것 (전부 관리자/내부 소유 데이터, 운영 미접촉)

| 항목 | 내용 | 결과 |
|---|---|---|
| deep audit 100% 전량검증 처리 | 골든 15건 (goldenrun10, `deep_audit=True` — 전 프레임 병렬 디코드 검증) | 15/15 통과, validate+deep audit 포함 finish 정상 |
| 실영상 전체 파이프라인 | 관리자 소유 기준 원본(177초) — 공식 벤치 25건 (warm10/cold5/burst10) | 25/25 성공, Layer1 검증 전 건 통과 |
| 실제 대기열 경로 | 접수→처리→취소→중복거부→러너 사망 복구 (Phase E) | 전 시나리오 통과 |
| 강등·복구 리허설 | RC0 강등 49s / 복원 46s | 5분 게이트 통과 |

합계: V32 전체 파이프라인 실행 44건(177초 25 + 골든 15 + 큐 4), 실패 0,
손상 출력 유출 0 (validator가 전 건 서명 URL 발급 전 검증).

## C0에서 아직 안 한 것 (대표 참여 필요 — GO 결정과 무관하게 권장)

1. **대표가 새 영상을 실제 업로드**해 처리 결과를 직접 육안 확인 (관리자 계정,
   프론트 경로). 지금까지는 기존 소유 영상의 재처리만 검증됨.
2. 운영 프론트가 staging 앱을 호출하게 하는 연결(관리자 계정 한정)은 운영 코드
   수정이 필요해 **대표 승인 전 미실시** (운영 main 코드 수정 금지 원칙).

## C0 운영 파라미터 (전환 시)

- deep_audit: 관리자 작업 100% (finish input 플래그, 이미 구현·검증됨)
- 실패 시: FAIL 기록 + 서명 URL 미발급 (고객 노출 0) + 강등 절차(49초) 대기
- 관측 항목: 콜드 배정 지연 빈도(cold-4형), un-blend 박스 영상 육안 표본
