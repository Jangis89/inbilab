# RC4 Phase C·F 진행 기록 (2026-08-21, 세션 2)

## 이번 세션에서 확정된 것 (전부 스테이징 실측)

### 1. INC-4 (g02 900s 행) — 해결 (확인됨)
- 원인: rc4 flow 경로가 h29.restore_chunk에 청크 내부 None 마스크를 전달
  → 함수 스레드 무보고 소멸. 수정: None→zeros 정화 (RC4-8).
- 검증: g02 121s 통과, 골든 35종 재검증에서 기존 25/25 PASS (회귀 0).

### 2. Phase C 통합 (RC4-10)
- effect-aware Locator: 키프레임 마스크를 외곽선·그림자·halo까지 확장
  (TELEA-差 기반, 2.5× 과확장 가드). 로컬 검증: 그림자 99% 포착.
- Preserver 자물쇠: 합성 단계에서 수정 허용 영역(paste∪카드 matte∪박스)
  밖은 원본 화소 강제 복귀. 로컬 검증: 밖 오염 0.00 복귀.
- 골든 영향: 기존 통과 골든 무회귀 (g02/g05 유지).

### 3. Phase F 카드 하이브리드 v1 (RC4-12)
- 밝은 카드 un-blend 일괄 기각(run91 veto) → **실측 검증 게이트**로 대체:
  후보 (s,t)를 키 8장에 실제 적용, 경계 연속성(원본 대비 절반↓)과
  질감 경사도(0.35~2.8×)를 통과해야 채택. 분산비 보조 추정 추가.
- 강한 un-blend(gain>3)에 경량 노이즈 억제(bilateral).
- 실측: **g34 FAIL→PASS**, g27 내부 18.7→20.2dB(질감 hf 0.29→0.85),
  g31 내부 +α. g02/g05 무회귀.

### 4. 반투명 중앙 카드 Locator (RC4-14) — g26/UAT-02 계열
- 3중 증거(시간축 분산 감쇠 + 국소 밝기 상승 + 내부 글자)로만 제안.
  불투명 고정 간판(실물 가능성)은 의도적으로 제외 — 오삭제 방지.
- 로컬 3케이스(카드/무카드/불투명간판) 전부 정판정.
- 실측: **g26 SCAN_FAIL(no_target) → static_card0 검출·처리 성공**
  (in 17.58dB — 최초 출력 확보, 품질은 다음 반복). g02 오탐 0.

## 현재 골든 상태 (35종 기준)
- 기존 g01~g25: 25/25 PASS
- 복원 g26~g35: g29/g30/g32/g35/**g34** PASS(5) / g26·g27·g28·g31·g33 FAIL(5)
- 남은 5종의 갭: 내부 질감 선명도(AI 뭉갬)·카드 내부 복원 — 명세 Phase G
  (라이선스 확인된 diffusion 후보 ROI 벤치) 또는 Phase I(자체 refiner 학습) 몫.

## 다음 단계 (우선순위)
1. g26/g27 카드 내부 품질: un-blend 기반 위 residual AI(글자 자리만) 정밀화,
   카드 스캔 추정(cb.s) 연동.
2. g28/g31/g33: crop_hq 게이트 완화 A/B + line layer 정밀도 확인.
3. Phase G: FloED(가중치 조항 확인 후) ROI 벤치 준비 — MiniMax 대비.
4. UAT-02/01 재처리는 위 반복 1~2회 후 (원본 보존 완료로 언제든 가능).

## 비용 누계(이번 세션): goldenrun GPU 사이클 8회(각 4×L40S 3~22분),
## deploy 6회(각 <1분), preserve/contact 3회(CPU only).

## 추가 반복 (RC4-16/17, g26 카드 심화)
- 카드 Locator가 스캔 수준 (s,t)를 `card_blends` 힌트로 세그에 전달 —
  파트별로 un-blend/AI가 갈리던 문제(시간축 불일치·카드 유령) 해소.
  boxstat 실측: 전 파트 box_unblend=2/box_ai=0/box_scan_blend 발동.
- 버그 수정: 노이즈 바닥을 전역 P10으로 잡으면 카드 내부(저분산)에 걸려
  s가 0으로 붕괴 → 링 기반 P10(상한 3.0)으로 교체.
- g26 지표: in 17.58→18.83dB, hf 0.741→0.903. 단 **glyph 잔존 629→1738**:
  un-blend(×2)가 카드 위 어두운 글자의 잔상도 증폭 — 글자 자리 AI 재도색
  범위/순서가 다음 과제. 시각적으로 카드 몸체는 제거 방향으로 전진.
- 남은 5종(g26·g27·g28·g31·g33)의 공통 병목 = 복원부 질감/잔상 →
  **Phase G(라이선스 확인된 diffusion 후보 ROI 벤치) 착수가 다음 큰 단계.**


## 마감 상태 (RC4-18, 2026-08-21 세션 2 종료 시점)
- g26 최종: SCAN_FAIL → 19.05dB (sharp 0.588, hf 0.871). 카드 몸체는 벗겨져
  배경이 비치기 시작. 잔여: 글자 잔상(glyph 1542)·flicker — un-blend 위
  글자 자리 재도색 품질이 병목. **MiniMax 미세조정 금지 원칙에 따라 이
  계열은 Phase G(상업 가능 diffusion 후보 ROI 벤치)로 이관.**
- t 재보정(RC4-18): 파트 실측 경계 일치(글자 화소 제외) — 로컬 검증에서
  t 오차 0.1 이내 복원. s 재추정(경사도비)은 노이즈 편향으로 기각.
- 브랜치 최종 커밋: RC4-18 (handler_v32 sha 7f74b702). staging 배포 완료.
- 골든 총괄: 기존 25/25 PASS + g34 PASS = 30/35. 잔여 5종(g26·g27·g28·
  g31·g33) 전부 '복원부 질감/잔상' 계열.
